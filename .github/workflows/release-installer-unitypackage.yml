name: Release Installer Unitypackage

on:
  push:
    tags:
      - "vlivekit-installer-v*"
  workflow_dispatch:
    inputs:
      version:
        description: Installer version, for example 0.0.1
        required: true
        type: string
      tag:
        description: Release tag. Uses vlivekit-installer-v<version> when empty.
        required: false
        type: string
      draft:
        description: Create the GitHub release as a draft
        required: true
        default: true
        type: boolean
      prerelease:
        description: Mark the GitHub release as a prerelease
        required: true
        default: false
        type: boolean

permissions:
  contents: write

env:
  EXPORT_SOURCE_PATH: Assets/VLiveKitInstaller.cs
  PACKAGE_NAME: VLiveKitInstaller

jobs:
  release:
    name: Export and publish installer unitypackage
    runs-on: [self-hosted, Windows]

    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve release inputs
        id: release
        run: |
          if ($env:GITHUB_EVENT_NAME -eq "push") {
            $tag = $env:GITHUB_REF_NAME
            if ($tag -notmatch '^vlivekit-installer-v(?<version>[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?)$') {
              throw "Tag must look like vlivekit-installer-v0.0.1: $tag"
            }

            $version = $Matches.version
            $draft = "false"
            $prerelease = "$($version -match '-')"
          } else {
            $version = "${{ inputs.version }}"
            if ($version -notmatch '^[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$') {
              throw "Version must look like 0.0.1 or 0.0.1-preview.1: $version"
            }

            $tag = "${{ inputs.tag }}"
            if (-not $tag) {
              $tag = "vlivekit-installer-v$version"
            }

            $draft = "${{ inputs.draft }}"
            $prerelease = "${{ inputs.prerelease }}"
          }

          if (-not (Test-Path $env:EXPORT_SOURCE_PATH)) {
            throw "Installer source was not found: $env:EXPORT_SOURCE_PATH"
          }

          $distPath = Join-Path $env:GITHUB_WORKSPACE "dist"
          $unityPackagePath = Join-Path $distPath "$($env:PACKAGE_NAME)-$version.unitypackage"
          $sha256Path = "$unityPackagePath.sha256"

          New-Item -ItemType Directory -Force -Path $distPath | Out-Null

          "version=$version" >> $env:GITHUB_OUTPUT
          "tag=$tag" >> $env:GITHUB_OUTPUT
          "draft=$draft" >> $env:GITHUB_OUTPUT
          "prerelease=$prerelease" >> $env:GITHUB_OUTPUT
          "unitypackage_path=$unityPackagePath" >> $env:GITHUB_OUTPUT
          "sha256_path=$sha256Path" >> $env:GITHUB_OUTPUT

      - name: Install release exporter
        run: |
          $editorPath = Join-Path "Assets" "Editor"
          New-Item -ItemType Directory -Force -Path $editorPath | Out-Null
          Copy-Item ".github/unity/VLiveKitUnityPackageExporter.cs" (Join-Path $editorPath "VLiveKitUnityPackageExporter.cs") -Force

      - name: Resolve Unity editor
        id: unity
        run: |
          if ($env:UNITY_EDITOR_PATH) {
            $unityPath = $env:UNITY_EDITOR_PATH
          } else {
            $projectVersion = (Get-Content "ProjectSettings/ProjectVersion.txt" | Select-String "m_EditorVersion:").ToString().Split(":")[1].Trim()
            $unityPath = "C:\Program Files\Unity\Hub\Editor\$projectVersion\Editor\Unity.exe"
          }

          if (-not (Test-Path $unityPath)) {
            throw "Unity editor was not found. Set UNITY_EDITOR_PATH or install the project Unity version. Tried: $unityPath"
          }

          "unity_path=$unityPath" >> $env:GITHUB_OUTPUT

      - name: Export unitypackage
        run: |
          $arguments = @(
            "-batchmode",
            "-quit",
            "-nographics",
            "-projectPath",
            $env:GITHUB_WORKSPACE,
            "-executeMethod",
            "VLiveKit.Release.UnityPackageExporter.Export",
            "-exportPackageSourcePath",
            $env:EXPORT_SOURCE_PATH,
            "-exportPackageOutputPath",
            "${{ steps.release.outputs.unitypackage_path }}",
            "-exportPackageIncludeDependencies",
            "false"
          )

          $process = Start-Process -FilePath "${{ steps.unity.outputs.unity_path }}" -ArgumentList $arguments -Wait -PassThru -WindowStyle Hidden
          if ($process.ExitCode -ne 0) {
            throw "Unity export failed with exit code $($process.ExitCode)"
          }

      - name: Create checksum
        run: |
          $hash = Get-FileHash "${{ steps.release.outputs.unitypackage_path }}" -Algorithm SHA256
          "$($hash.Hash.ToLowerInvariant())  $(Split-Path "${{ steps.release.outputs.unitypackage_path }}" -Leaf)" | Set-Content "${{ steps.release.outputs.sha256_path }}" -NoNewline

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release.outputs.tag }}
          name: VLiveKit Installer ${{ steps.release.outputs.version }}
          draft: ${{ steps.release.outputs.draft }}
          prerelease: ${{ steps.release.outputs.prerelease }}
          generate_release_notes: true
          files: |
            ${{ steps.release.outputs.unitypackage_path }}
            ${{ steps.release.outputs.sha256_path }}
